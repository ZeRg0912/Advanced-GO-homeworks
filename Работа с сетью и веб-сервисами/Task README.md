## Навык, который вы закрепите
· Создание полноценного API на Go.\
· Использование маршрутов, обработчиков и сериализации данных (JSON).\
· Подготовка сервера с пользовательской документацией (README/OpenAPI по желанию).

## Инструменты и материалы
· Go 1.20+ (рекомендуется 1.21+), Git, любой редактор кода.\
· HTTP‑клиент: curl или Postman.\
· Разрешённый стек: стандартная библиотека net/http или роутер (chi, gorilla/mux).\
· Учебные материалы модуля: «Запуск HTTP‑сервера и маршрутизация в Go», «Обработка запросов и REST API», презентация «Микросервисная архитектура».

## Задание
Реализуйте REST API для управления списком задач (to‑do). Сервис работает в памяти (без БД) и отдаёт/принимает только JSON.

## Требуемые эндпоинты
· GET /tasks — получить список задач.\
· POST /tasks — создать задачу.\
· GET /tasks/{id} — получить задачу по идентификатору.\
· PUT /tasks/{id} — обновить задачу целиком.\
· DELETE /tasks/{id} — удалить задачу.

Контракт данных
```
Task {
  "id": number,        // генерируется на сервере
  "title": string,     // обязательное поле
  "done": boolean,     // статус выполнения
  "created_at": string // ISO8601, по желанию
}
```

## Нефункциональные требования
· Ответы и ошибки — в JSON. Заголовок: Content-Type: application/json.\
· Коды статусов: 200/201/204 для успеха; 400 для неверных данных; 404 для отсутствующих ресурсов; 405 при неподдерживаемом методе; 500 при внутренних ошибках.\
· Единый формат ошибок, например: { “error”: “сообщение” }.\
· Хранение: in‑memory (синхронизация по необходимости).\
· Логи входящих запросов (минимум: метод и путь).\
· PRO по желанию: GET /health (health‑check) и описание API (таблица эндпоинтов в README или OpenAPI‑файл).

## Шаблон проекта (скелет — без реализаций)
```
tasks-api/
  cmd/server/main.go
  internal/handlers/tasks.go        // TODO: реализовать логику
  internal/models/task.go           // модель данных
  internal/storage/storage.go       // интерфейс Storage
  internal/http/middleware.go       // (опционально)
  README.md
  go.mod
Тонкий каркас (starter)
Даём только минимальные файлы и сигнатуры. Вся бизнес‑логика — на студенте.
cmd/server/main.go (минимум для запуска)
package main

import (
    "log"
    "net/http"

    "tasks-api/internal/handlers"
    "tasks-api/internal/storage"
)

func main() {
    // TODO: подключите конкретную реализацию (in‑memory) интерфейса Storage
    var store storage.Storage // = memory.New() // реализуйте сами

    h := handlers.New(store)

    mux := http.NewServeMux()
    mux.HandleFunc("/tasks", h.TasksCollection) // GET, POST
    mux.HandleFunc("/tasks/", h.TaskItem)       // GET, PUT, DELETE

    log.Println("server listening on :8080")
    if err := http.ListenAndServe(":8080", mux); err != nil {
        log.Fatal(err)
    }
}
internal/storage/storage.go (интерфейс, без реализации)
package storage

import "tasks-api/internal/models"

type Storage interface {
    List() []models.Task
    Create(models.Task) (models.Task, error)
    Get(id int) (models.Task, bool)
    Update(id int, models.Task) (models.Task, error)
    Delete(id int) error
}
internal/handlers/tasks.go (заглушки обработчиков)
package handlers

import (
    "net/http"
    "tasks-api/internal/storage"
)

type Handler struct{ Store storage.Storage }

func New(s storage.Storage) *Handler { return &Handler{Store: s} }

// /tasks (GET, POST)
func (h *Handler) TasksCollection(w http.ResponseWriter, r *http.Request) {
    // TODO: реализуйте разбор метода, JSON, коды статусов, валидацию
}

// /tasks/{id} (GET, PUT, DELETE)
func (h *Handler) TaskItem(w http.ResponseWriter, r *http.Request) {
    // TODO: извлечение id, маршрутизация по методу, ошибки
}
internal/models/task.go (модель)
package models

type Task struct {
    ID        int    `json:"id"`
    Title     string `json:"title"`
    Done      bool   `json:"done"`
    CreatedAt string `json:"created_at,omitempty"`
}
README.md — оставить как в задании (запуск + примеры curl).
```

## Инструкция к выполнению
1. Склонируйте/создайте проект из скелета выше и инициализируйте go mod init tasks-api.
2. Реализуйте модель Task и in‑memory хранилище по интерфейсу Storage (самостоятельная реализация, потокобезопасность обязательна).
3. Настройте HTTP‑сервер (net/http или выбранный роутер). Зарегистрируйте маршруты коллекции и элемента.
4. Реализуйте хендлеры для всех методов. Обеспечьте корректные коды статусов и заголовки.
5. Сведите все ответы к JSON (включая ошибки). Введите единый формат ошибок.
6. Добавьте базовое логирование запросов.
7. Проведите ручное тестирование: выполните для каждого эндпоинта по одному корректному и по одному ошибочному сценарию, зафиксируйте команды curl и ответы.
8. Подготовьте README: как запустить, таблицу эндпоинтов, примеры запрос/ответ.
9. (Опционально) Добавьте GET /health и/или описание API (таблица, Postman‑коллекция или OpenAPI‑файл). Добавьте синхронизацию при доступе из нескольких обработчиков.
10. Настройте HTTP‑сервер (net/http или выбранный роутер). Зарегистрируйте маршруты коллекции и элемента.
11. Реализуйте хендлеры для всех методов. Обеспечьте корректные коды статусов и заголовки.
12. Сведите все ответы к JSON (включая ошибки). Введите единый формат ошибок.
13. Добавьте базовое логирование запросов.
14. Проведите ручное тестирование: выполните для каждого эндпоинта по одному корректному и по одному ошибочному сценарию, зафиксируйте команды curl и ответы.
15. Подготовьте README: как запустить, таблицу эндпоинтов, примеры запрос/ответ.
16. (Опционально) Добавьте GET /health и/или описание API (таблица, Postman‑коллекция или OpenAPI‑файл).

## Как отправить на проверку
Загрузите в ответ ссылку на репозиторий (GitHub/GitLab) или архив проекта, а также:\
· README.md с инструкцией запуска и документацией по эндпоинтам.\
· Файл manual-tests.md или раздел в README с командами curl и примерными ответами.\
· (Опционально) Postman‑коллекцию или OpenAPI‑файл.

## Критерии оценки (для задания с проверкой)

## Зачёт
· Реализованы все 5 эндпоинтов с корректными методами и кодами статусов.\
· Ответы и ошибки — в JSON с правильным Content-Type.\
· Настроена маршрутизация; хранилище реализовано самостоятельно (in‑memory), корректно генерируются id, операции потокобезопасны.\
· Обработчики реализованы без готовой логики из заготовки; соблюдена валидация входящих данных.\
· Есть документация (README: запуск, таблица эндпоинтов, примеры curl).\
· Проект собирается и запускается с первого раза по инструкции из README.

## Незачёт
· Пустое решение или повторная отправка без учёта правок.